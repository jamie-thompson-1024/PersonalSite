
import { ProjectInfo, Filter } from './ProjectInfo';

import './SortFilterProjects.css';

function SortFilterProjects(props: { onChange: (filter: Filter) => void })
{
    return (
        <div className="SortFilterProjects">
            
        </div>
    )
}

export default SortFilterProjects;
